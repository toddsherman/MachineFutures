{
  "name": "machine-futures",
  "private": true,
  "description": "A public record of what frontier AI models forecast about humanity's relationship with AI across multiple time horizons.",
  "scripts": {
    "check": "node tools/check-all.mjs",
    "test:browser": "playwright test",
    "serve": "python3 -m http.server 4173 --directory public"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
