{
  "name": "machine-futures",
  "private": true,
  "description": "A public record of what frontier AI models think about the long-run end state of humanity's relationship with AI.",
  "scripts": {
    "check": "node tools/check-all.mjs",
    "test:browser": "playwright test",
    "serve": "python3 -m http.server 4173 --directory public"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
