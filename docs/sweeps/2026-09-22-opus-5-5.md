# September 22, 2026 Opus 5.5 addition

The owner requested a new Claude Opus 5.5 entry using the project questions. The [addition plan](2026-09-22-opus-5-5.json), committed before collection, freezes one exact API model (`claude-opus-5-5`), all eleven horizons, their prompt identities and hashes, the collection date, and 20 accepted samples per horizon.

This is an individual model addition, not a full-cohort refresh. Previously published models retain their existing raw batches and collection dates. The generated site combines each model's latest result per horizon, as the existing importer already does. Opus 5.5 belongs to Anthropic; adding it changes Anthropic's internal mean without increasing that lab's total weight.

- API preflight passed in [35763243711](https://github.com/toddsherman/MachineFutures/actions/runs/35763243711).
- Collection: [35763308453](https://github.com/toddsherman/MachineFutures/actions/runs/35763308453).
- Required evidence: 11 complete raw batches and 220 accepted samples.

The collection workflow's full-roster publication gate is expected to reject this model-only dispatch because the other models are not being purchased again. Publication of this owner-requested addition instead requires the precommitted addition plan to pass the same sweep validator, plus the full data-integrity, reproducibility, and browser checks. The scheduled full-cohort gate remains unchanged. Raw evidence and original workflow artifacts are retained.

To validate this addition, invoke `tools/check-sweep.mjs --plan-json` with the addition manifest's contents and `--runs runs`.
