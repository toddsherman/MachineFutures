## What changed

<!-- Describe the user-visible, data, prompt, or maintenance change. -->

## Data and prompt impact

- [ ] No published data or prompt changed.
- [ ] Raw batches were added to `runs/` and pass integrity checks.
- [ ] Collapsed raw-batch diffs or their CI integrity/sweep summaries were reviewed.
- [ ] Generated outputs were rebuilt with `npm run data:rebuild`.
- [ ] A prompt changed and its `questionSet` version was reviewed.

## Verification

<!-- List the checks run and any manual review performed. -->
